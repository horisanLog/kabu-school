# 株式投資スクール Web サイト ルーティング設計

**作成日：2025 年 10 月 31 日**  
**バージョン：1.0**  
**ステータス：初版ドラフト**  
**前提文書：要件定義.md、情報設計\_サイトマップ.md、コンテンツ設計.md、デザイン指針.md、データ設計.md**

---

## 📋 プロジェクト概要

| 項目             | 内容                                                  |
| ---------------- | ----------------------------------------------------- |
| **サイトテーマ** | 株式投資スクール                                      |
| **使用技術**     | Next.js 14 (Pages Router) + TypeScript + Tailwind CSS |
| **CMS**          | 使用しない（静的 JSON 管理）                          |
| **対応デバイス** | PC / タブレット / スマートフォン（SP ファースト）     |
| **外部連携**     | マイページ（外部ドメイン）、外部 LP 連携可能          |

---

## 1. ルーティング表（URL 戦略）

### 1-1. 全ページ一覧

| パス              | 目的                   | 主 CTA                     | 外部遷移 | 優先度 | 備考                              |
| ----------------- | ---------------------- | -------------------------- | -------- | ------ | --------------------------------- |
| `/`               | LP：理解 → 信頼 → 行動 | 無料体験申込（`/seminar`） | 任意     | 🔴 高  | 固定 CTA（モバイル）              |
| `/curriculum`     | カリキュラム詳細       | 体験申込（`/seminar`）     | なし     | 🔴 高  | 15 回タイムライン                 |
| `/schedule`       | 開催検索 → 申込        | 各カードの「申し込む」     | 可       | 🔴 高  | クエリ：`?date=&location=`        |
| `/textbook`       | 教材の特長訴求         | 体験申込（`/seminar`）     | なし     | 🟡 中  | 画像は `/public/images`           |
| `/seminar`        | 体験申込 LP            | 申込フォームへ             | 可       | 🔴 高  | 自前フォーム or 外部 LP           |
| `/apply`          | 申込フォーム           | 送信                       | なし     | 🔴 高  | `/apply/confirm`, `/apply/thanks` |
| `/apply/confirm`  | 申込内容確認           | 送信                       | なし     | 🔴 高  | -                                 |
| `/apply/thanks`   | 申込完了               | LINE 登録、資料 DL         | なし     | 🔴 高  | サンクスページ                    |
| `/voice`          | 受講生の声一覧         | 詳細 → 申込                | なし     | 🟡 中  | カテゴリフィルター                |
| `/voice/[id]`     | 受講生の声詳細         | 申込（`/seminar`）         | なし     | 🟡 中  | 動的ルート                        |
| `/case-study`     | 成功事例一覧           | 申込                       | なし     | 🟢 低  | -                                 |
| `/faq`            | よくある質問（全件）   | お問い合わせ、申込         | なし     | 🟡 中  | アコーディオン                    |
| `/blog`           | ブログ記事一覧         | 記事詳細、メルマガ登録     | なし     | 🟡 中  | カテゴリ・タグフィルター          |
| `/blog/[slug]`    | ブログ記事詳細         | 関連記事、申込             | なし     | 🟡 中  | 動的ルート                        |
| `/media`          | メディア掲載実績       | トップへ戻る               | なし     | 🟢 低  | -                                 |
| `/contact`        | お問い合わせフォーム   | 送信                       | なし     | 🟡 中  | `/contact/thanks`                 |
| `/contact/thanks` | お問い合わせ完了       | トップへ戻る               | なし     | 🟡 中  | -                                 |
| `/login`          | 受講生ログイン導線     | 外部マイページへ           | 必須     | 🟡 中  | `https://mypage.example.com`      |
| `/company`        | 会社概要               | お問い合わせ               | なし     | 🟢 低  | -                                 |
| `/privacy`        | プライバシーポリシー   | なし                       | なし     | 🔴 高  | 法的要件                          |
| `/terms`          | 利用規約               | なし                       | なし     | 🟡 中  | 法的要件                          |
| `/tokushoho`      | 特定商取引法           | なし                       | なし     | 🔴 高  | 法的要件                          |
| `/disclaimer`     | 投資リスク免責事項     | なし                       | なし     | 🔴 高  | 法的要件                          |
| `/sitemap`        | サイトマップ（HTML）   | 各ページへのリンク         | なし     | 🟢 低  | -                                 |

### 1-2. クエリパラメータ一覧

| ページ      | パラメータ    | 型      | 説明                          | 例                       |
| ----------- | ------------- | ------- | ----------------------------- | ------------------------ |
| `/schedule` | `date`        | string  | 年月でフィルター              | `?date=2025-11`          |
|             | `location`    | string  | 地域でフィルター              | `?location=tokyo`        |
|             | `online`      | boolean | オンライン絞り込み            | `?online=true`           |
| `/apply`    | `schedule_id` | string  | スケジュール ID（プリセット） | `?schedule_id=event-001` |
| `/voice`    | `category`    | string  | カテゴリでフィルター          | `?category=beginner`     |
| `/blog`     | `category`    | string  | カテゴリでフィルター          | `?category=basic`        |
|             | `tag`         | string  | タグでフィルター              | `?tag=NISA`              |

---

## 2. ページ骨子（各ページのセクション順・主要コンポーネント）

### 2-1. `/` トップページ（LP）

#### セクション順

1. Hero（ヒーロー）
2. Stats（実績）
3. Benefits（3 つの強み）
4. Curriculum Digest（カリキュラム概要）
5. Lead Strip（無料体験誘導）
6. Reviews（受講生の声）
7. Instructors（講師紹介）
8. Schedule Teaser（開催スケジュール抜粋）
9. FAQ（よくある質問抜粋）
10. Final CTA（最終 CTA）

#### 主要コンポーネント

- `<Hero />`
- `<Stats />`
- `<BenefitGrid />`
- `<CurriculumDigest />`
- `<LeadStrip />`
- `<ReviewList />`
- `<InstructorCards />`
- `<ScheduleTeaser />`
- `<FAQAccordion />`
- `<FinalCTA />`
- `<CTAStickyBar />` ← SP のみ表示

#### DOM 骨子

```tsx
// pages/index.tsx
import { GetStaticProps } from "next"

interface HomePageProps {
  settings: Settings
  reviews: Review[]
  instructors: Instructor[]
  events: Event[]
  faqs: FAQ[]
}

export default function HomePage({
  settings,
  reviews,
  instructors,
  events,
  faqs,
}: HomePageProps) {
  return (
    <main>
      {/* Hero Section */}
      <section id="hero" className="py-16 md:py-24 bg-white">
        <Hero />
      </section>

      {/* Stats Section */}
      <section id="stats" className="py-12 md:py-16 bg-gray-50">
        <Stats data={settings.stats} />
      </section>

      {/* Benefits Section */}
      <section id="benefits" className="py-16 md:py-24 bg-white">
        <BenefitGrid />
      </section>

      {/* Curriculum Digest */}
      <section id="curriculum" className="py-16 md:py-24 bg-gray-50">
        <CurriculumDigest />
      </section>

      {/* Lead Strip */}
      <section id="lead" className="py-12 md:py-16 bg-accent text-white">
        <LeadStrip />
      </section>

      {/* Reviews */}
      <section id="reviews" className="py-16 md:py-24 bg-white">
        <ReviewList reviews={reviews} />
      </section>

      {/* Instructors */}
      <section id="instructors" className="py-16 md:py-24 bg-gray-50">
        <InstructorCards instructors={instructors} />
      </section>

      {/* Schedule Teaser */}
      <section id="schedule" className="py-16 md:py-24 bg-white">
        <ScheduleTeaser events={events.slice(0, 3)} />
      </section>

      {/* FAQ */}
      <section id="faq" className="py-16 md:py-24 bg-gray-50">
        <FAQAccordion faqs={faqs} />
      </section>

      {/* Final CTA */}
      <section id="cta" className="py-16 md:py-24 bg-accent text-white">
        <FinalCTA />
      </section>

      {/* Sticky CTA Bar (SP only) */}
      <CTAStickyBar />
    </main>
  )
}

export const getStaticProps: GetStaticProps<HomePageProps> = async () => {
  const settings = getSettings()
  const reviews = getFeaturedReviews(3)
  const instructors = getFeaturedInstructors()
  const events = getAvailableEvents()
  const faqs = getFeaturedFAQs(5)

  return {
    props: {
      settings,
      reviews,
      instructors,
      events,
      faqs,
    },
    revalidate: 3600, // 1時間ごとに再生成
  }
}
```

#### 計測イベント

- `pv_home` - ページビュー
- `click_cta_hero` - ヒーロー CTA クリック
- `click_cta_benefits` - 強みセクション CTA クリック
- `click_cta_final` - 最終 CTA クリック
- `click_sticky_cta` - 固定バー CTA クリック（SP）
- `accordion_faq_open` - FAQ 開閉
- `click_review_more` - 受講生の声「もっと見る」

---

### 2-2. `/curriculum` カリキュラム詳細ページ

#### セクション順

1. Page Hero（ページヒーロー）
2. Overview（概要イントロ）
3. Timeline 15（15 回タイムライン）
4. Outcomes（到達目標）
5. Format & Period（受講形態・期間）
6. Textbook Preview（教材紹介）
7. Tuition（受講料）
8. CTA Inline（体験申込 CTA）

#### 主要コンポーネント

- `<PageHero />`
- `<Overview />`
- `<Timeline15 />`
- `<OutcomesTable />`
- `<FormatPeriod />`
- `<TextbookPreview />`
- `<Tuition />`
- `<CTAInline />`

#### DOM 骨子

```tsx
// app/curriculum/page.tsx
export default async function CurriculumPage() {
  const basicLessons = await getLessonsByPhase("basic")
  const practicalLessons = await getLessonsByPhase("practical")
  const advancedLessons = await getLessonsByPhase("advanced")
  const textbooks = await getTextbooks()

  return (
    <main>
      {/* Page Hero */}
      <section className="py-16 md:py-24 bg-gray-50">
        <PageHero
          title="15回で完成する、実践的カリキュラム"
          subtitle="初心者が投資家として自立するための、体系的な学習プログラム"
        />
      </section>

      {/* Overview */}
      <section className="py-16 md:py-24 bg-white">
        <Overview />
      </section>

      {/* Timeline */}
      <section className="py-16 md:py-24 bg-gray-50">
        <Timeline15
          basicLessons={basicLessons}
          practicalLessons={practicalLessons}
          advancedLessons={advancedLessons}
        />
      </section>

      {/* Format & Period */}
      <section className="py-16 md:py-24 bg-white">
        <FormatPeriod />
      </section>

      {/* Textbook Preview */}
      <section className="py-12 md:py-16 bg-gray-50">
        <TextbookPreview textbooks={textbooks} />
      </section>

      {/* Tuition */}
      <section className="py-16 md:py-24 bg-white">
        <Tuition />
      </section>

      {/* CTA */}
      <section className="py-12 md:py-16 bg-accent text-white">
        <CTAInline
          title="まずは無料体験で、授業の雰囲気を体感してください"
          buttonText="無料体験セミナーに申し込む"
          href="/seminar"
        />
      </section>
    </main>
  )
}
```

#### 計測イベント

- `pv_curriculum` - ページビュー
- `click_timeline_expand` - タイムライン展開
- `click_cta_curriculum` - CTA クリック
- `click_textbook_link` - 教材ページ遷移

---

### 2-3. `/schedule` 開催スケジュールページ

#### セクション順

1. Page Hero
2. Schedule Filter（絞り込み）
3. Schedule List（結果カードリスト）
4. Empty State（該当なしの場合）
5. CTA（希望日程がない場合の問い合わせ導線）

#### 主要コンポーネント

- `<PageHero />`
- `<ScheduleFilter />`
- `<ScheduleList />`
- `<EmptyState />`
- `<CTAInline />`

#### DOM 骨子

```tsx
// app/schedule/page.tsx
"use client"

import { useSearchParams } from "next/navigation"
import { useEffect, useState } from "react"

export default function SchedulePage() {
  const searchParams = useSearchParams()
  const [events, setEvents] = useState<Event[]>([])
  const [loading, setLoading] = useState(true)

  // フィルター条件
  const date = searchParams.get("date") || ""
  const location = searchParams.get("location") || "all"
  const online = searchParams.get("online") === "true"

  useEffect(() => {
    // データフェッチ & フィルタリング
    fetchAndFilterEvents({ date, location, online })
  }, [date, location, online])

  return (
    <main>
      {/* Page Hero */}
      <section className="py-16 md:py-24 bg-gray-50">
        <PageHero
          title="無料体験セミナー 開催スケジュール"
          subtitle="ご都合の良い日程をお選びください"
        />
      </section>

      {/* Filter */}
      <section className="py-8 bg-white sticky top-16 z-10 shadow-sm">
        <ScheduleFilter />
      </section>

      {/* Results */}
      <section className="py-12 md:py-16 bg-gray-50">
        <div className="max-w-screen-lg mx-auto px-4">
          {loading ? (
            <LoadingState />
          ) : events.length > 0 ? (
            <ScheduleList events={events} />
          ) : (
            <EmptyState />
          )}
        </div>
      </section>

      {/* CTA */}
      <section className="py-12 md:py-16 bg-white">
        <CTAInline
          title="希望の日程が見つからない場合"
          description="個別相談も承ります。お気軽にお問い合わせください。"
          buttonText="お問い合わせフォームへ"
          href="/contact"
        />
      </section>
    </main>
  )
}
```

#### 計測イベント

- `pv_schedule` - ページビュー
- `filter_schedule_date` - 日付フィルター変更
- `filter_schedule_location` - 地域フィルター変更
- `filter_schedule_online` - オンラインフィルター変更
- `click_apply_schedule` - 各スケジュールの申込クリック

---

### 2-4. `/textbook` 教材紹介ページ

#### セクション順

1. Textbook Hero（教材ヒーロー）
2. Features（3 つの特長）
3. Sample Pages（サンプルページ）
4. TOC（目次ダイジェスト）
5. Authors（制作チーム）
6. CTA

#### 主要コンポーネント

- `<TextbookHero />`
- `<FeatureTriad />`
- `<SamplePages />`
- `<TOCExcerpt />`
- `<Authors />`
- `<CTAInline />`

#### DOM 骨子

```tsx
// app/textbook/page.tsx
export default async function TextbookPage() {
  const textbooks = await getTextbooks()
  const mainTextbook = textbooks[0] // メイン教材

  return (
    <main>
      {/* Hero */}
      <section className="py-16 md:py-24 bg-gray-50">
        <TextbookHero textbook={mainTextbook} />
      </section>

      {/* Features */}
      <section className="py-16 md:py-24 bg-white">
        <FeatureTriad features={mainTextbook.features} />
      </section>

      {/* Sample Pages */}
      <section className="py-16 md:py-24 bg-gray-50">
        <SamplePages images={mainTextbook.images} />
      </section>

      {/* TOC */}
      <section className="py-16 md:py-24 bg-white">
        <TOCExcerpt toc={mainTextbook.tableOfContents} />
      </section>

      {/* Authors */}
      {mainTextbook.authors && (
        <section className="py-12 md:py-16 bg-gray-50">
          <Authors authors={mainTextbook.authors} />
        </section>
      )}

      {/* CTA */}
      <section className="py-12 md:py-16 bg-accent text-white">
        <CTAInline
          title="この教材を使ったカリキュラムを体験する"
          buttonText="無料体験セミナーに申し込む"
          href="/seminar"
        />
      </section>
    </main>
  )
}
```

#### 計測イベント

- `pv_textbook` - ページビュー
- `click_sample_image` - サンプル画像拡大
- `click_cta_textbook` - CTA クリック

---

### 2-5. `/seminar` 無料体験セミナー LP

#### セクション順

1. Seminar Hero
2. Recommended For（こんな方におすすめ）
3. Seminar Flow（セミナーの流れ）
4. Benefits（参加特典）
5. Testimonials（参加者の声）
6. Instructor Info（講師紹介）
7. FAQ（セミナー限定 FAQ）
8. Schedule Teaser（開催スケジュール）
9. Final CTA

#### 主要コンポーネント

- `<SeminarHero />`
- `<RecommendedFor />`
- `<SeminarFlow />`
- `<Benefits />`
- `<Testimonials />`
- `<InstructorInfo />`
- `<FAQAccordion />`
- `<ScheduleTeaser />`
- `<FinalCTA />`

#### DOM 骨子

```tsx
// app/seminar/page.tsx
export default async function SeminarPage() {
  const events = await getAvailableEvents()
  const instructors = await getFeaturedInstructors()

  return (
    <main>
      {/* Hero */}
      <section className="py-16 md:py-24 bg-white">
        <SeminarHero />
      </section>

      {/* Recommended For */}
      <section className="py-16 md:py-24 bg-gray-50">
        <RecommendedFor />
      </section>

      {/* Seminar Flow */}
      <section className="py-16 md:py-24 bg-white">
        <SeminarFlow />
      </section>

      {/* Benefits */}
      <section className="py-12 md:py-16 bg-accent text-white">
        <Benefits />
      </section>

      {/* Testimonials */}
      <section className="py-16 md:py-24 bg-white">
        <Testimonials />
      </section>

      {/* Instructor */}
      <section className="py-16 md:py-24 bg-gray-50">
        <InstructorInfo instructor={instructors[0]} />
      </section>

      {/* FAQ */}
      <section className="py-16 md:py-24 bg-white">
        <FAQAccordion category="seminar" />
      </section>

      {/* Schedule */}
      <section className="py-16 md:py-24 bg-gray-50">
        <ScheduleTeaser events={events} />
      </section>

      {/* Final CTA */}
      <section className="py-16 md:py-24 bg-accent text-white">
        <FinalCTA
          title="投資の第一歩を、今日から始めませんか？"
          buttonText="無料体験セミナーに申し込む"
          href="/apply"
        />
      </section>

      {/* Sticky CTA */}
      <CTAStickyBar />
    </main>
  )
}
```

#### 計測イベント

- `pv_seminar` - ページビュー
- `click_cta_seminar_hero` - ヒーロー CTA
- `click_cta_seminar_final` - 最終 CTA
- `click_schedule_from_seminar` - スケジュールから申込

---

### 2-6. `/apply` 申込フォームページ

#### セクション順

1. Page Hero
2. Application Form（申込フォーム）
3. Privacy Policy Agreement
4. Submit Button

#### 主要コンポーネント

- `<PageHero />`
- `<ApplicationForm />`
- `<ProgressBar />`

#### DOM 骨子

```tsx
// app/apply/page.tsx
"use client"

import { useSearchParams } from "next/navigation"

export default function ApplyPage() {
  const searchParams = useSearchParams()
  const scheduleId = searchParams.get("schedule_id")

  return (
    <main>
      {/* Progress Bar */}
      <ProgressBar currentStep={1} totalSteps={3} />

      {/* Page Hero */}
      <section className="py-12 md:py-16 bg-white">
        <PageHero
          title="無料体験セミナー 申込フォーム"
          subtitle="ステップ 1：情報入力"
        />
      </section>

      {/* Form */}
      <section className="py-12 md:py-16 bg-gray-50">
        <div className="max-w-2xl mx-auto px-4">
          <ApplicationForm preselectedScheduleId={scheduleId} />
        </div>
      </section>
    </main>
  )
}
```

#### 計測イベント

- `pv_apply` - ページビュー
- `form_start` - フォーム入力開始
- `form_field_focus` - 各フィールドフォーカス
- `form_error` - バリデーションエラー
- `click_confirm` - 確認画面へ進む

---

### 2-7. `/apply/confirm` 申込確認ページ

```tsx
// app/apply/confirm/page.tsx
"use client"

export default function ApplyConfirmPage() {
  return (
    <main>
      <ProgressBar currentStep={2} totalSteps={3} />

      <section className="py-12 md:py-16 bg-white">
        <PageHero title="入力内容の確認" subtitle="ステップ 2：確認" />
      </section>

      <section className="py-12 md:py-16 bg-gray-50">
        <div className="max-w-2xl mx-auto px-4">
          <ConfirmationView />
          <div className="flex gap-4 mt-8">
            <button className="btn-secondary">← 修正する</button>
            <button className="btn-primary flex-1">送信する →</button>
          </div>
        </div>
      </section>
    </main>
  )
}
```

---

### 2-8. `/apply/thanks` 申込完了ページ

```tsx
// app/apply/thanks/page.tsx
export default function ApplyThanksPage() {
  return (
    <main>
      <ProgressBar currentStep={3} totalSteps={3} />

      <section className="py-16 md:py-24 bg-white text-center">
        <div className="max-w-2xl mx-auto px-4">
          <div className="mb-8">
            <div className="inline-flex items-center justify-center w-20 h-20 bg-green-100 rounded-full mb-4">
              <CheckCircle className="w-12 h-12 text-green-500" />
            </div>
            <h1 className="text-3xl font-bold text-gray-800 mb-4">
              お申し込みありがとうございました！
            </h1>
            <p className="text-lg text-gray-600">
              ご登録のメールアドレスに確認メールをお送りしました
            </p>
          </div>

          {/* Next Steps */}
          <NextSteps />

          {/* Reservation Details */}
          <ReservationDetails />

          {/* Secondary CTAs */}
          <SecondaryActions />
        </div>
      </section>
    </main>
  )
}
```

#### 計測イベント

- `pv_apply_thanks` - 完了ページビュー（コンバージョン）
- `click_line_register` - LINE 登録
- `click_download_material` - 資料ダウンロード

---

### 2-9. `/voice` 受講生の声一覧ページ

```tsx
// app/voice/page.tsx
export default async function VoicePage() {
  const reviews = await getReviews()

  return (
    <main>
      <section className="py-16 md:py-24 bg-gray-50">
        <PageHero title="受講生の声" />
      </section>

      <section className="py-8 bg-white sticky top-16 z-10">
        <CategoryFilter
          categories={["全て", "初心者", "中級者", "女性", "会社員"]}
        />
      </section>

      <section className="py-12 md:py-16 bg-gray-50">
        <ReviewGrid reviews={reviews} />
      </section>

      <section className="py-12 md:py-16 bg-white">
        <CTAInline
          title="あなたも無料体験で、第一歩を踏み出しませんか？"
          buttonText="無料体験セミナーに申し込む"
          href="/seminar"
        />
      </section>
    </main>
  )
}
```

---

### 2-10. `/voice/[id]` 受講生の声詳細ページ

```tsx
// app/voice/[id]/page.tsx
export default async function VoiceDetailPage({
  params,
}: {
  params: { id: string }
}) {
  const review = await getReviewById(params.id)

  if (!review) {
    notFound()
  }

  return (
    <main>
      <Breadcrumb items={["ホーム", "受講生の声", review.name]} />

      <section className="py-16 md:py-24 bg-white">
        <ReviewDetail review={review} />
      </section>

      <section className="py-12 md:py-16 bg-gray-50">
        <RelatedReviews category={review.categories[0]} limit={3} />
      </section>

      <section className="py-12 md:py-16 bg-accent text-white">
        <CTAInline
          title="あなたも無料体験で、投資の第一歩を"
          buttonText="無料体験セミナーに申し込む"
          href="/seminar"
        />
      </section>
    </main>
  )
}
```

---

### 2-11. `/blog` ブログ一覧ページ

```tsx
// app/blog/page.tsx
export default async function BlogPage() {
  const posts = await getBlogPosts()
  const featuredPosts = posts.filter((p) => p.featured).slice(0, 3)
  const recentPosts = posts.slice(0, 10)

  return (
    <main>
      <section className="py-16 md:py-24 bg-gray-50">
        <PageHero title="ブログ" subtitle="投資の基礎知識から市場分析まで" />
      </section>

      {/* Featured Posts */}
      <section className="py-12 md:py-16 bg-white">
        <FeaturedPosts posts={featuredPosts} />
      </section>

      {/* Category Filter */}
      <section className="py-8 bg-gray-50 sticky top-16 z-10">
        <CategoryFilter />
      </section>

      {/* Post List */}
      <section className="py-12 md:py-16 bg-gray-50">
        <BlogGrid posts={recentPosts} />
      </section>
    </main>
  )
}
```

---

### 2-12. `/login` ログイン導線ページ

```tsx
// app/login/page.tsx
export default function LoginPage() {
  return (
    <main>
      <section className="py-16 md:py-24 bg-white">
        <div className="max-w-2xl mx-auto px-4 text-center">
          <h1 className="text-3xl font-bold text-gray-800 mb-4">
            受講生マイページ
          </h1>
          <p className="text-lg text-gray-600 mb-8">
            既に受講されている方は、こちらからログインしてください
          </p>

          <ExternalLoginCard
            title="マイページへログイン"
            description="教材の閲覧、スケジュール確認、質問投稿などが可能です"
            url="https://mypage.example.com"
            buttonText="ログインページへ"
          />

          <div className="mt-12 pt-12 border-t border-gray-200">
            <p className="text-gray-600 mb-4">まだ受講されていない方</p>
            <a href="/seminar" className="btn-primary">
              無料体験セミナーに申し込む
            </a>
          </div>
        </div>
      </section>
    </main>
  )
}
```

---

## 3. Next.js ファイル構成（Pages Router）

```
/pages
├── _app.tsx                      # カスタムApp
├── _document.tsx                 # カスタムDocument
├── index.tsx                     # トップページ (/)
├── curriculum/
│   └── index.tsx                 # カリキュラム (/curriculum)
├── schedule/
│   └── index.tsx                 # スケジュール (/schedule)
├── textbook/
│   └── index.tsx                 # 教材紹介 (/textbook)
├── seminar/
│   └── index.tsx                 # セミナーLP (/seminar)
├── apply/
│   ├── index.tsx                 # 申込フォーム (/apply)
│   ├── confirm.tsx               # 確認画面 (/apply/confirm)
│   └── thanks.tsx                # 完了画面 (/apply/thanks)
├── voice/
│   ├── index.tsx                 # 受講生の声一覧 (/voice)
│   └── [id].tsx                  # 受講生の声詳細 (/voice/[id])
├── case-study/
│   └── index.tsx                 # 成功事例 (/case-study)
├── faq/
│   └── index.tsx                 # FAQ (/faq)
├── blog/
│   ├── index.tsx                 # ブログ一覧 (/blog)
│   └── [slug].tsx                # ブログ記事詳細 (/blog/[slug])
├── media/
│   └── index.tsx                 # メディア掲載 (/media)
├── contact/
│   ├── index.tsx                 # お問い合わせ (/contact)
│   └── thanks.tsx                # 完了画面 (/contact/thanks)
├── login/
│   └── index.tsx                 # ログイン導線 (/login)
├── company/
│   └── index.tsx                 # 会社概要 (/company)
├── privacy/
│   └── index.tsx                 # プライバシーポリシー (/privacy)
├── terms/
│   └── index.tsx                 # 利用規約 (/terms)
├── tokushoho/
│   └── index.tsx                 # 特定商取引法 (/tokushoho)
├── disclaimer/
│   └── index.tsx                 # 免責事項 (/disclaimer)
├── sitemap/
│   └── index.tsx                 # サイトマップ (/sitemap)
└── 404.tsx                       # 404ページ

/components
├── layout/
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── Breadcrumb.tsx
│   └── CTAStickyBar.tsx
├── home/
│   ├── Hero.tsx
│   ├── Stats.tsx
│   ├── BenefitGrid.tsx
│   ├── CurriculumDigest.tsx
│   ├── LeadStrip.tsx
│   ├── ReviewList.tsx
│   ├── InstructorCards.tsx
│   ├── ScheduleTeaser.tsx
│   └── FinalCTA.tsx
├── curriculum/
│   ├── PageHero.tsx
│   ├── Overview.tsx
│   ├── Timeline15.tsx
│   ├── OutcomesTable.tsx
│   ├── FormatPeriod.tsx
│   ├── TextbookPreview.tsx
│   └── Tuition.tsx
├── schedule/
│   ├── ScheduleFilter.tsx
│   ├── ScheduleList.tsx
│   ├── ScheduleCard.tsx
│   └── EmptyState.tsx
├── textbook/
│   ├── TextbookHero.tsx
│   ├── FeatureTriad.tsx
│   ├── SamplePages.tsx
│   ├── TOCExcerpt.tsx
│   └── Authors.tsx
├── seminar/
│   ├── SeminarHero.tsx
│   ├── RecommendedFor.tsx
│   ├── SeminarFlow.tsx
│   ├── Benefits.tsx
│   ├── Testimonials.tsx
│   └── InstructorInfo.tsx
├── apply/
│   ├── ApplicationForm.tsx
│   ├── ProgressBar.tsx
│   ├── ConfirmationView.tsx
│   ├── NextSteps.tsx
│   ├── ReservationDetails.tsx
│   └── SecondaryActions.tsx
├── voice/
│   ├── ReviewGrid.tsx
│   ├── ReviewCard.tsx
│   ├── ReviewDetail.tsx
│   ├── RelatedReviews.tsx
│   └── CategoryFilter.tsx
├── blog/
│   ├── FeaturedPosts.tsx
│   ├── BlogGrid.tsx
│   ├── BlogCard.tsx
│   └── BlogContent.tsx
├── shared/
│   ├── CTAInline.tsx
│   ├── FAQAccordion.tsx
│   ├── LoadingState.tsx
│   └── ExternalLoginCard.tsx
└── ui/
    ├── Button.tsx
    ├── Input.tsx
    ├── Select.tsx
    ├── Textarea.tsx
    ├── Checkbox.tsx
    └── RadioGroup.tsx
```

---

## 4. ナビゲーション設計

### 4-1. グローバルナビゲーション（PC）

```tsx
// components/layout/Header.tsx
const navItems = [
  { label: "カリキュラム", href: "/curriculum" },
  { label: "開催スケジュール", href: "/schedule" },
  { label: "受講生の声", href: "/voice" },
  { label: "ブログ", href: "/blog" },
  { label: "FAQ", href: "/faq" },
  { label: "ログイン", href: "/login" },
]

export function Header() {
  return (
    <header className="sticky top-0 z-50 bg-white shadow-sm">
      <nav className="max-w-screen-xl mx-auto px-4 h-16 flex items-center justify-between">
        {/* Logo */}
        <Link href="/" className="text-2xl font-bold text-accent">
          株式投資スクール
        </Link>

        {/* Nav Items */}
        <ul className="hidden md:flex items-center gap-6">
          {navItems.map((item) => (
            <li key={item.href}>
              <Link
                href={item.href}
                className="text-gray-700 hover:text-accent"
              >
                {item.label}
              </Link>
            </li>
          ))}
        </ul>

        {/* CTA */}
        <Link href="/seminar" className="btn-primary">
          無料体験申込
        </Link>

        {/* Mobile Menu Toggle */}
        <button className="md:hidden">
          <Menu className="w-6 h-6" />
        </button>
      </nav>
    </header>
  )
}
```

### 4-2. フッターナビゲーション

```tsx
// components/layout/Footer.tsx
const footerSections = [
  {
    title: "スクールについて",
    links: [
      { label: "カリキュラム詳細", href: "/curriculum" },
      { label: "教材紹介", href: "/textbook" },
      { label: "受講生の声", href: "/voice" },
      { label: "成功事例", href: "/case-study" },
      { label: "メディア掲載", href: "/media" },
    ],
  },
  {
    title: "参加する",
    links: [
      { label: "開催スケジュール", href: "/schedule" },
      { label: "無料体験セミナー", href: "/seminar" },
      { label: "申込フォーム", href: "/apply" },
    ],
  },
  {
    title: "サポート",
    links: [
      { label: "FAQ", href: "/faq" },
      { label: "お問い合わせ", href: "/contact" },
      { label: "ブログ", href: "/blog" },
      { label: "ログイン", href: "/login" },
    ],
  },
  {
    title: "企業情報",
    links: [
      { label: "会社概要", href: "/company" },
      { label: "プライバシーポリシー", href: "/privacy" },
      { label: "利用規約", href: "/terms" },
      { label: "特定商取引法", href: "/tokushoho" },
      { label: "免責事項", href: "/disclaimer" },
    ],
  },
]

export function Footer() {
  return (
    <footer className="bg-gray-800 text-white">
      <div className="max-w-screen-xl mx-auto px-4 py-12">
        <div className="grid grid-cols-2 md:grid-cols-4 gap-8">
          {footerSections.map((section) => (
            <div key={section.title}>
              <h3 className="font-semibold mb-4">{section.title}</h3>
              <ul className="space-y-2">
                {section.links.map((link) => (
                  <li key={link.href}>
                    <Link
                      href={link.href}
                      className="text-gray-300 hover:text-white text-sm"
                    >
                      {link.label}
                    </Link>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>

        {/* SNS Links */}
        <div className="mt-8 pt-8 border-t border-gray-700">
          <SocialLinks />
        </div>

        {/* Copyright */}
        <div className="mt-8 text-center text-sm text-gray-400">
          © 2025 株式投資スクール. All Rights Reserved.
        </div>
      </div>
    </footer>
  )
}
```

### 4-3. パンくずリスト

```tsx
// components/layout/Breadcrumb.tsx
interface BreadcrumbProps {
  items: string[]
}

export function Breadcrumb({ items }: BreadcrumbProps) {
  return (
    <nav className="py-4 px-4 bg-gray-50">
      <ol className="max-w-screen-lg mx-auto flex items-center gap-2 text-sm">
        <li>
          <Link href="/" className="text-accent hover:underline">
            ホーム
          </Link>
        </li>
        {items.map((item, index) => (
          <li key={index} className="flex items-center gap-2">
            <ChevronRight className="w-4 h-4 text-gray-400" />
            {index === items.length - 1 ? (
              <span className="text-gray-600">{item}</span>
            ) : (
              <span className="text-accent hover:underline">{item}</span>
            )}
          </li>
        ))}
      </ol>
    </nav>
  )
}
```

---

## 5. 外部連携・リンク方針

### 5-1. 外部 LP 連携

```tsx
// 外部LPへのリンク
<a
  href="https://external-lp.example.com/seminar"
  target="_blank"
  rel="noopener noreferrer"
  className="btn-primary"
  onClick={() => trackEvent("click_external_lp")}
>
  申し込む
</a>
```

### 5-2. マイページ導線

```tsx
// app/login/page.tsx
const MYPAGE_URL = "https://mypage.example.com"

export default function LoginPage() {
  return (
    <ExternalLoginCard
      url={MYPAGE_URL}
      title="マイページへログイン"
      onClick={() => trackEvent("click_mypage_login")}
    />
  )
}
```

### 5-3. スケジュールからの外部 LP 遷移

```tsx
// components/schedule/ScheduleCard.tsx
<a
  href={event.entryUrl}
  target={event.entryUrl.startsWith("http") ? "_blank" : undefined}
  rel={event.entryUrl.startsWith("http") ? "noopener noreferrer" : undefined}
  className="btn-primary"
>
  この日程で申し込む
</a>
```

---

## 6. SEO・OGP・構造化データ

### 6-1. メタデータ設定

```tsx
// pages/index.tsx
import Head from "next/head"

export default function HomePage() {
  return (
    <>
      <Head>
        <title>株式投資スクール | 投資を、学ぶ力に変える</title>
        <meta
          name="description"
          content="初心者から始める株式投資スクール。実践的な15回カリキュラムで、自分で判断できる投資家へ。無料体験セミナー受付中。"
        />

        {/* OGP */}
        <meta
          property="og:title"
          content="株式投資スクール | 投資を、学ぶ力に変える"
        />
        <meta
          property="og:description"
          content="初心者から始める株式投資スクール。実践的な15回カリキュラムで、自分で判断できる投資家へ。"
        />
        <meta property="og:url" content="https://example.com" />
        <meta property="og:site_name" content="株式投資スクール" />
        <meta
          property="og:image"
          content="https://example.com/images/og-image.jpg"
        />
        <meta property="og:image:width" content="1200" />
        <meta property="og:image:height" content="630" />
        <meta property="og:locale" content="ja_JP" />
        <meta property="og:type" content="website" />

        {/* Twitter Card */}
        <meta name="twitter:card" content="summary_large_image" />
        <meta
          name="twitter:title"
          content="株式投資スクール | 投資を、学ぶ力に変える"
        />
        <meta
          name="twitter:description"
          content="初心者から始める株式投資スクール。"
        />
        <meta
          name="twitter:image"
          content="https://example.com/images/og-image.jpg"
        />
      </Head>

      {/* ページコンテンツ */}
      <main>{/* ... */}</main>
    </>
  )
}
```

### 6-2. 構造化データ

```tsx
// pages/curriculum/index.tsx
import Head from "next/head"

export default function CurriculumPage() {
  const courseSchema = {
    "@context": "https://schema.org",
    "@type": "Course",
    name: "株式投資スクール 15回カリキュラム",
    description: "初心者が投資家として自立するための体系的な学習プログラム",
    provider: {
      "@type": "Organization",
      name: "株式投資スクール",
      sameAs: "https://example.com",
    },
    hasCourseInstance: {
      "@type": "CourseInstance",
      courseMode: ["online", "onsite"],
      duration: "P4M",
      instructor: {
        "@type": "Person",
        name: "山田 太郎",
      },
    },
    offers: {
      "@type": "Offer",
      price: "298000",
      priceCurrency: "JPY",
    },
  }

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(courseSchema) }}
      />
      {/* ... */}
    </>
  )
}
```

### 6-3. FAQ 構造化データ

```tsx
// app/faq/page.tsx
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((faq) => ({
    "@type": "Question",
    name: faq.question,
    acceptedAnswer: {
      "@type": "Answer",
      text: faq.answer,
    },
  })),
}
```

---

## 7. エラーページとユーティリティ

### 7-1. 404 ページ

```tsx
// app/not-found.tsx
export default function NotFound() {
  return (
    <main className="py-16 md:py-24 text-center">
      <div className="max-w-2xl mx-auto px-4">
        <h1 className="text-6xl font-bold text-gray-800 mb-4">404</h1>
        <p className="text-xl text-gray-600 mb-8">
          お探しのページが見つかりませんでした
        </p>

        <div className="space-y-4">
          <p className="text-gray-600">以下のページもご覧ください：</p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Link href="/" className="btn-secondary">
              トップページへ
            </Link>
            <Link href="/seminar" className="btn-primary">
              無料体験セミナー
            </Link>
            <Link href="/curriculum" className="btn-secondary">
              カリキュラム
            </Link>
          </div>
        </div>
      </div>
    </main>
  )
}
```

### 7-2. エラーページ

```tsx
// app/error.tsx
"use client"

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <main className="py-16 md:py-24 text-center">
      <div className="max-w-2xl mx-auto px-4">
        <h1 className="text-4xl font-bold text-gray-800 mb-4">
          エラーが発生しました
        </h1>
        <p className="text-gray-600 mb-8">
          申し訳ございません。一時的なエラーが発生しました。
        </p>

        <div className="space-y-4">
          <button onClick={reset} className="btn-primary">
            もう一度試す
          </button>
          <Link href="/" className="btn-secondary block">
            トップページへ戻る
          </Link>
        </div>
      </div>
    </main>
  )
}
```

### 7-3. ローディング

```tsx
// app/loading.tsx
export default function Loading() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="w-12 h-12 border-4 border-accent border-t-transparent rounded-full animate-spin" />
    </div>
  )
}
```

---

## 8. イベント計測設計

### 8-1. イベント命名規則

```ts
// lib/analytics.ts
export const AnalyticsEvents = {
  // ページビュー
  PV_HOME: "pv_home",
  PV_CURRICULUM: "pv_curriculum",
  PV_SCHEDULE: "pv_schedule",
  PV_SEMINAR: "pv_seminar",
  PV_APPLY: "pv_apply",
  PV_APPLY_THANKS: "pv_apply_thanks", // コンバージョン

  // CTAクリック
  CLICK_CTA_HERO: "click_cta_hero",
  CLICK_CTA_BENEFITS: "click_cta_benefits",
  CLICK_CTA_CURRICULUM: "click_cta_curriculum",
  CLICK_CTA_FINAL: "click_cta_final",
  CLICK_STICKY_CTA: "click_sticky_cta",
  CLICK_APPLY_SCHEDULE: "click_apply_schedule",

  // フィルター操作
  FILTER_SCHEDULE_DATE: "filter_schedule_date",
  FILTER_SCHEDULE_LOCATION: "filter_schedule_location",
  FILTER_SCHEDULE_ONLINE: "filter_schedule_online",
  FILTER_VOICE_CATEGORY: "filter_voice_category",

  // FAQ
  FAQ_OPEN: "faq_open",
  FAQ_CLOSE: "faq_close",

  // フォーム
  FORM_START: "form_start",
  FORM_FIELD_FOCUS: "form_field_focus",
  FORM_ERROR: "form_error",
  FORM_CONFIRM: "form_confirm",
  FORM_SUBMIT: "form_submit",

  // 外部リンク
  CLICK_EXTERNAL_LP: "click_external_lp",
  CLICK_MYPAGE_LOGIN: "click_mypage_login",

  // 二次CTA
  CLICK_LINE_REGISTER: "click_line_register",
  CLICK_DOWNLOAD_MATERIAL: "click_download_material",
} as const

export function trackEvent(eventName: string, params?: Record<string, any>) {
  if (typeof window !== "undefined" && window.gtag) {
    window.gtag("event", eventName, params)
  }

  // Google Tag Manager
  if (typeof window !== "undefined" && window.dataLayer) {
    window.dataLayer.push({
      event: eventName,
      ...params,
    })
  }
}
```

### 8-2. 計測実装例

```tsx
// components/home/Hero.tsx
import { trackEvent, AnalyticsEvents } from "@/lib/analytics"

export function Hero() {
  return (
    <section>
      <a
        href="/seminar"
        className="btn-primary"
        onClick={() => trackEvent(AnalyticsEvents.CLICK_CTA_HERO)}
      >
        無料体験セミナーに申し込む
      </a>
    </section>
  )
}
```

---

## 9. 実装テンプレート

### 9-1. 基本ページテンプレート

```tsx
// app/[page]/page.tsx
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "ページタイトル | 株式投資スクール",
  description: "ページ説明文",
}

export default async function PageName() {
  // データ取得
  const data = await fetchData()

  return (
    <main>
      {/* ページヒーロー */}
      <section className="py-16 md:py-24 bg-gray-50">
        <PageHero title="ページタイトル" />
      </section>

      {/* コンテンツセクション */}
      <section className="py-16 md:py-24 bg-white">
        <ContentSection data={data} />
      </section>

      {/* CTA */}
      <section className="py-12 md:py-16 bg-accent text-white">
        <CTAInline
          title="無料体験で、投資の第一歩を"
          buttonText="無料体験セミナーに申し込む"
          href="/seminar"
        />
      </section>
    </main>
  )
}
```

---

**📌 備考**

- このルーティング設計は Pages Router（Next.js 14）を前提としています
- 全 23 ページのルーティングと骨子を定義しました
- 外部連携（マイページ、外部 LP）の方針を明確化しました
- イベント計測の命名規則と実装方法を統一しました

---

**🔗 関連ドキュメント**

- `要件定義.md`（前提文書）
- `情報設計_サイトマップ.md`（前提文書）
- `コンテンツ設計.md`（前提文書）
- `デザイン指針.md`（前提文書）
- `データ設計.md`（前提文書）
- `実装ガイド.md`（次フェーズで作成）

# 株式投資スクール Web サイト データ設計

**作成日：2025 年 10 月 31 日**  
**バージョン：1.0**  
**ステータス：初版ドラフト**  
**前提文書：要件定義.md、情報設計\_サイトマップ.md、コンテンツ設計.md、デザイン指針.md**

---

## 📋 プロジェクト概要

| 項目             | 内容                                                 |
| ---------------- | ---------------------------------------------------- |
| **サイトテーマ** | 株式投資スクール                                     |
| **使用技術**     | Next.js 14 (Pages Router) + TypeScript + Tailwind CSS |
| **データ管理**   | ローカル JSON ファイル（CMS なし）                   |
| **格納場所**     | `/data` ディレクトリ                                 |
| **型安全性**     | TypeScript + Zod バリデーション                      |

---

## 1. データ構成概要

### データモデル一覧

| モデル         | 目的                     | データ数目安 | 管理形式                  | 更新頻度 | 優先度 |
| -------------- | ------------------------ | ------------ | ------------------------- | -------- | ------ |
| **Event**      | 開催スケジュール・申込   | 10〜30 件    | `/data/events.json`       | 週次     | 🔴 高  |
| **Lesson**     | 15 回分のカリキュラム    | 15 件固定    | `/data/lessons.json`      | 低       | 🔴 高  |
| **Review**     | 受講生の声・評価         | 10〜30 件    | `/data/reviews.json`      | 月次     | 🔴 高  |
| **FAQ**        | よくある質問             | 20〜30 件    | `/data/faq.json`          | 月次     | 🟡 中  |
| **Textbook**   | 教材紹介                 | 3〜5 件      | `/data/textbooks.json`    | 低       | 🟡 中  |
| **Instructor** | 講師紹介                 | 2〜5 名      | `/data/instructors.json`  | 低       | 🟡 中  |
| **Media**      | メディア掲載実績         | 5〜15 件     | `/data/media.json`        | 月次     | 🟢 低  |
| **CaseStudy**  | 成功事例                 | 5〜10 件     | `/data/case-studies.json` | 低       | 🟢 低  |
| **BlogPost**   | ブログ記事メタデータ     | 10〜50 件    | `/data/blog-posts.json`   | 週次     | 🟡 中  |
| **Settings**   | サイト全体設定（共通値） | 1 件         | `/data/settings.json`     | 低       | 🔴 高  |

### ディレクトリ構成

```
/
├── data/
│   ├── events.json           # 開催スケジュール
│   ├── lessons.json          # カリキュラム
│   ├── reviews.json          # 受講生の声
│   ├── faq.json              # よくある質問
│   ├── textbooks.json        # 教材紹介
│   ├── instructors.json      # 講師情報
│   ├── media.json            # メディア掲載
│   ├── case-studies.json     # 成功事例
│   ├── blog-posts.json       # ブログ記事一覧
│   └── settings.json         # サイト設定
│
├── types/
│   ├── index.ts              # 型定義エクスポート
│   ├── event.ts              # Event型
│   ├── lesson.ts             # Lesson型
│   ├── review.ts             # Review型
│   ├── faq.ts                # FAQ型
│   ├── textbook.ts           # Textbook型
│   ├── instructor.ts         # Instructor型
│   ├── media.ts              # Media型
│   ├── case-study.ts         # CaseStudy型
│   ├── blog.ts               # BlogPost型
│   └── settings.ts           # Settings型
│
├── lib/
│   ├── data-loader.ts        # データ読み込みユーティリティ
│   └── validators.ts         # Zodバリデーション
│
└── public/
    └── images/               # 画像ファイル
        ├── instructors/
        ├── reviews/
        ├── textbooks/
        ├── media-logos/
        └── blog/
```

---

## 2. TypeScript 型定義

### 2-1. Event（開催スケジュール）

```ts
// types/event.ts
export type EventStatus = 'available' | 'few' | 'full' | 'closed';
export type EventLocation = 'online' | 'tokyo' | 'osaka';

export interface Event {
  id: string;
  title: string;
  date: string; // ISO8601 (例: "2025-11-15")
  startTime: string; // "19:00"
  endTime: string; // "20:30"
  location: EventLocation;
  venue: string; // "Zoom" or "東京会場"
  instructor: string; // 講師名
  capacity: number; // 定員
  remainingSeats: number; // 残席数
  status: EventStatus;
  description?: string; // 詳細説明（任意）
  entryUrl: string; // 申込URL
  tags?: string[]; // タグ（例: ["初心者向け", "オンライン"]）
  createdAt: string; // 作成日
  updatedAt: string; // 更新日
}
```

### 2-2. Lesson（カリキュラム）

```ts
// types/lesson.ts
export type LessonPhase = 'basic' | 'practical' | 'advanced';

export interface Lesson {
  no: number; // 回数（1〜15）
  phase: LessonPhase; // 基礎・実践・応用
  title: string;
  summary: string; // 1行説明
  description: string; // 詳細説明
  learningObjectives: string[]; // 学習目標
  outcomes: string[]; // 到達目標
  topics: string[]; // 学習トピック
  tools?: string[]; // 使用ツール（任意）
  duration: number; // 所要時間（分）
  order: number; // 表示順序
}
```

### 2-3. Review（受講生の声）

```ts
// types/review.ts
export type ReviewCategory = 'beginner' | 'intermediate' | 'female' | 'businessman' | 'self-employed';

export interface Review {
  id: string;
  name: string; // 表示名（例："田中 由美 様"）
  age?: number; // 年齢（任意）
  occupation: string; // 職業
  photo?: string; // 顔写真URL（任意）
  rating: number; // 評価（1〜5）
  commentShort: string; // 短いコメント（100文字・一覧用）
  commentLong: string; // 詳細コメント（500文字・詳細ページ用）
  beforeStatus: string; // 受講前の状況
  afterStatus: string; // 受講後の変化
  categories: ReviewCategory[]; // カテゴリ
  attendancePeriod?: string; // 受講期間（例："2024年4月〜8月"）
  featured: boolean; // トップページに表示するか
  order: number; // 表示順序
  publishedAt: string; // 公開日
}
```

### 2-4. FAQ（よくある質問）

```ts
// types/faq.ts
export type FAQCategory = 'fee' | 'content' | 'format' | 'support' | 'refund' | 'other';

export interface FAQ {
  id: string;
  category: FAQCategory;
  categoryLabel: string; // カテゴリ表示名（例："料金について"）
  question: string;
  answer: string;
  order: number; // カテゴリ内での表示順序
  featured: boolean; // トップページに表示するか
  tags?: string[]; // タグ（検索用）
  relatedFaqIds?: string[]; // 関連FAQ
  createdAt: string;
  updatedAt: string;
}
```

### 2-5. Textbook（教材紹介）

```ts
// types/textbook.ts
export interface Textbook {
  id: string;
  title: string;
  subtitle?: string;
  description: string;
  images: string[]; // 画像URL配列（表紙、見開きなど）
  features: string[]; // 特徴（3〜5項目）
  targetLevel: string; // 対象レベル（例："初心者〜中級者"）
  pageCount: number; // ページ数
  format: string; // 形式（例："A5サイズ・フルカラー"）
  tableOfContents: string[]; // 目次
  authors?: string[]; // 著者・監修者
  publishedDate?: string; // 発行日
  sampleUrl?: string; // サンプルPDF URL（任意）
  order: number;
}
```

### 2-6. Instructor（講師情報）

```ts
// types/instructor.ts
export interface Instructor {
  id: string;
  name: string;
  nameKana?: string; // 読み仮名
  title: string; // 肩書（例："証券アナリスト・CFP®"）
  photo: string; // 顔写真URL
  bio: string; // 経歴（200文字程度）
  message: string; // 受講生へのメッセージ（300文字程度）
  specialties: string[]; // 専門分野
  certifications?: string[]; // 保有資格
  careerYears?: number; // 経験年数
  socialLinks?: {
    twitter?: string;
    linkedin?: string;
    website?: string;
  };
  featured: boolean; // トップページに表示するか
  order: number;
}
```

### 2-7. Media（メディア掲載実績）

```ts
// types/media.ts
export interface Media {
  id: string;
  name: string; // メディア名
  logo: string; // ロゴ画像URL
  category: string; // カテゴリ（例："新聞", "Web", "テレビ"）
  publishedDate: string; // 掲載日
  title?: string; // 記事タイトル
  url?: string; // 記事URL（任意）
  description?: string; // 掲載内容説明
  order: number;
}
```

### 2-8. CaseStudy（成功事例）

```ts
// types/case-study.ts
export interface CaseStudy {
  id: string;
  title: string;
  studentName: string; // 受講生名（例："佐藤 様"）
  age?: number;
  occupation: string;
  photo?: string; // 写真URL
  beforeInvestment: string; // 受講前の投資状況
  afterInvestment: string; // 受講後の成果
  investmentResult?: string; // 投資成果（数値）
  story: string; // ストーリー詳細（1000文字程度）
  videoUrl?: string; // インタビュー動画URL（任意）
  testimonial: string; // 一言コメント
  publishedAt: string;
  order: number;
}
```

### 2-9. BlogPost（ブログ記事メタデータ）

```ts
// types/blog.ts
export type BlogCategory = 'basic' | 'market-analysis' | 'interview' | 'news' | 'tips';

export interface BlogPost {
  id: string;
  slug: string; // URLスラッグ
  title: string;
  excerpt: string; // 抜粋（150文字）
  content: string; // 本文（Markdown）
  category: BlogCategory;
  categoryLabel: string; // カテゴリ表示名
  tags: string[];
  featuredImage: string; // アイキャッチ画像URL
  author: string; // 著者名
  authorPhoto?: string; // 著者写真URL
  readTime: number; // 読了時間（分）
  featured: boolean; // おすすめ記事か
  publishedAt: string;
  updatedAt: string;
}
```

### 2-10. Settings（サイト設定）

```ts
// types/settings.ts
export interface Settings {
  site: {
    name: string; // サイト名
    description: string; // サイト説明
    url: string; // サイトURL
    ogImage: string; // OGP画像
  };
  contact: {
    phone: string; // 電話番号
    email: string; // メールアドレス
    businessHours: string; // 営業時間
  };
  stats: {
    totalStudents: number; // 累計受講者数
    satisfactionRate: number; // 満足度（%）
    continuationRate: number; // 継続率（%）
    partnerCompanies: number; // 提携企業数
  };
  social: {
    twitter?: string;
    facebook?: string;
    instagram?: string;
    youtube?: string;
    line?: string;
  };
  features: {
    blogEnabled: boolean; // ブログ機能有効化
    caseStudyEnabled: boolean; // 成功事例機能有効化
    multiLanguage: boolean; // 多言語対応
  };
}
```

### 型定義エクスポート（types/index.ts）

```ts
// types/index.ts
export * from './event';
export * from './lesson';
export * from './review';
export * from './faq';
export * from './textbook';
export * from './instructor';
export * from './media';
export * from './case-study';
export * from './blog';
export * from './settings';

// ユーティリティ型
export type Status = 'draft' | 'published' | 'archived';
export type DateString = string; // ISO8601形式
export type URLString = string;
```

---

## 3. JSON データサンプル

### 3-1. events.json

```json
[
  {
    "id": "event-001",
    "title": "11月度 無料体験セミナー",
    "date": "2025-11-15",
    "startTime": "19:00",
    "endTime": "20:30",
    "location": "online",
    "venue": "Zoom",
    "instructor": "山田 太郎",
    "capacity": 50,
    "remainingSeats": 12,
    "status": "available",
    "description": "初心者向けの90分無料体験セミナー。投資の基礎から当スクールのカリキュラムまで詳しくご紹介します。",
    "entryUrl": "/apply?schedule_id=event-001",
    "tags": ["初心者向け", "オンライン", "無料"],
    "createdAt": "2025-10-15T10:00:00Z",
    "updatedAt": "2025-10-30T14:30:00Z"
  },
  {
    "id": "event-002",
    "title": "東京会場 無料体験セミナー",
    "date": "2025-11-18",
    "startTime": "14:00",
    "endTime": "15:30",
    "location": "tokyo",
    "venue": "東京・丸の内校",
    "instructor": "佐藤 花子",
    "capacity": 20,
    "remainingSeats": 3,
    "status": "few",
    "description": "対面形式の無料体験セミナー。講師と直接対話できる貴重な機会です。",
    "entryUrl": "/apply?schedule_id=event-002",
    "tags": ["初心者向け", "対面", "東京"],
    "createdAt": "2025-10-15T10:00:00Z",
    "updatedAt": "2025-10-31T09:00:00Z"
  },
  {
    "id": "event-003",
    "title": "大阪会場 無料体験セミナー",
    "date": "2025-11-22",
    "startTime": "10:00",
    "endTime": "11:30",
    "location": "osaka",
    "venue": "大阪・梅田校",
    "instructor": "鈴木 一郎",
    "capacity": 15,
    "remainingSeats": 0,
    "status": "full",
    "description": "大阪開催の無料体験セミナー。関西在住の方におすすめです。",
    "entryUrl": "/apply?schedule_id=event-003",
    "tags": ["初心者向け", "対面", "大阪"],
    "createdAt": "2025-10-15T10:00:00Z",
    "updatedAt": "2025-10-25T16:00:00Z"
  }
]
```

### 3-2. lessons.json

```json
[
  {
    "no": 1,
    "phase": "basic",
    "title": "投資とは何か？株式市場の仕組み",
    "summary": "投資の本質と株式市場の役割を理解する",
    "description": "投資と投機の違い、株式市場がなぜ存在するのか、株価が動く理由について基礎から学びます。初心者が最初に理解すべき「投資の本質」を丁寧に解説します。",
    "learningObjectives": [
      "投資と投機の違いを説明できる",
      "株式市場の役割を理解する",
      "株価が変動する理由を説明できる"
    ],
    "outcomes": [
      "投資の本質を理解し、ギャンブルではないという正しい認識を持つ",
      "株式市場の仕組みを理解する"
    ],
    "topics": [
      "投資と投機の違い",
      "株式市場の役割",
      "株価が動く理由",
      "長期投資の重要性"
    ],
    "tools": ["証券会社の画面デモ", "市場データの見方"],
    "duration": 90,
    "order": 1
  },
  {
    "no": 2,
    "phase": "basic",
    "title": "NISA・iDeCoの活用法と税制メリット",
    "summary": "非課税制度を理解し、効果的に活用する",
    "description": "新NISA制度とiDeCoの仕組みを学び、税制優遇のメリットを最大限に活用する方法を理解します。自分に合った制度の選び方も解説します。",
    "learningObjectives": [
      "新NISA制度の仕組みを理解する",
      "iDeCoの特徴と活用法を理解する",
      "税制優遇のメリットを試算できる"
    ],
    "outcomes": [
      "自分に合った非課税制度を選択できる",
      "証券口座の選び方を理解する"
    ],
    "topics": [
      "新NISA制度の仕組み（つみたて投資枠・成長投資枠）",
      "iDeCoの特徴と活用法",
      "税制優遇のメリット試算",
      "証券口座の選び方"
    ],
    "tools": ["NISAシミュレーター", "各証券会社の比較表"],
    "duration": 90,
    "order": 2
  },
  {
    "no": 15,
    "phase": "advanced",
    "title": "投資計画の作成と卒業制作",
    "summary": "自分の投資計画を立て、自立した投資家へ",
    "description": "これまで学んだ知識を総動員して、自分の投資計画書を作成します。リスク許容度の設定、ポートフォリオ設計、今後の学習ロードマップを立て、卒業後も自立して投資を継続できる準備をします。",
    "learningObjectives": [
      "自分のリスク許容度を設定できる",
      "ポートフォリオを設計できる",
      "投資計画書を作成できる"
    ],
    "outcomes": [
      "卒業後も自立して投資を継続できる計画を立てる",
      "定期的な見直しの重要性を理解する"
    ],
    "topics": [
      "自分の投資計画書の作成",
      "リスク許容度の設定",
      "ポートフォリオ設計",
      "今後の学習ロードマップ"
    ],
    "tools": ["投資計画テンプレート", "ポートフォリオ管理ツール"],
    "duration": 90,
    "order": 15
  }
]
```

### 3-3. reviews.json

```json
[
  {
    "id": "review-001",
    "name": "田中 由美 様",
    "age": 38,
    "occupation": "会社員",
    "photo": "/images/reviews/tanaka.jpg",
    "rating": 5,
    "commentShort": "NISA口座を開設したものの、何を買えばいいのか全くわからず...。このスクールで基礎から学んだことで、自信を持って投資を始められました。",
    "commentLong": "30代後半になり、老後資金のことが気になり始めました。会社の同僚が「NISAいいよ」と言うので口座は開設したものの、何を買えばいいのか全くわからず、半年以上放置していました。ネットで調べても情報が多すぎて混乱...。「ちゃんと基礎から学びたい」と思い、このスクールの無料体験に参加しました。一番良かったのは、「なぜこの銘柄を選ぶのか」という判断基準が身についたことです。今では月3万円の積立投資を継続できています。",
    "beforeStatus": "投資経験ゼロ、NISA口座は放置",
    "afterStatus": "月3万円の積立投資を継続中",
    "categories": ["beginner", "female", "businessman"],
    "attendancePeriod": "2024年4月〜8月",
    "featured": true,
    "order": 1,
    "publishedAt": "2024-09-01T00:00:00Z"
  },
  {
    "id": "review-002",
    "name": "佐藤 健太 様",
    "age": 45,
    "occupation": "自営業",
    "photo": "/images/reviews/sato.jpg",
    "rating": 5,
    "commentShort": "仕事が忙しくオンライン受講を選択。録画でも視聴できるので、自分のペースで学べたのが良かったです。",
    "commentLong": "自営業で時間が不規則なため、オンライン受講を選びました。録画でも視聴できるので、深夜や早朝など自分のペースで学べたのが非常に助かりました。シミュレーション投資で失敗を経験できたことで、実践でも冷静に判断できるようになりました。講師の方も質問に丁寧に答えてくださり、理解が深まりました。",
    "beforeStatus": "独学で挫折、本を読んでも理解できず",
    "afterStatus": "シミュレーションで経験を積み、実践投資開始",
    "categories": ["intermediate", "self-employed"],
    "attendancePeriod": "2024年5月〜9月",
    "featured": true,
    "order": 2,
    "publishedAt": "2024-10-01T00:00:00Z"
  },
  {
    "id": "review-003",
    "name": "鈴木 真理子 様",
    "age": 32,
    "occupation": "主婦",
    "photo": "/images/reviews/suzuki.jpg",
    "rating": 4,
    "commentShort": "投資は男性がやるものだと思っていましたが、女性の受講生も多く安心。講師の方も丁寧で、初歩的な質問にも親切に答えてくれました。",
    "commentLong": "投資は難しそうで、男性がやるものだと思っていました。でも子どもの教育資金のことを考えると、何もしないわけにはいかない...。勇気を出して無料体験に参加したら、女性の受講生も多くて安心しました。講師の方も丁寧で、「こんな初歩的なこと聞いていいの？」という質問にも親切に答えてくれました。今では将来の子どもの教育資金を計画的に準備できそうです。",
    "beforeStatus": "投資に興味はあるが不安で行動できず",
    "afterStatus": "教育資金の計画的な準備を開始",
    "categories": ["beginner", "female"],
    "attendancePeriod": "2024年6月〜10月",
    "featured": true,
    "order": 3,
    "publishedAt": "2024-10-15T00:00:00Z"
  }
]
```

### 3-4. faq.json

```json
[
  {
    "id": "faq-001",
    "category": "content",
    "categoryLabel": "カリキュラム",
    "question": "投資経験がゼロでも大丈夫ですか？",
    "answer": "はい、問題ありません。受講生の約70%が投資未経験からスタートしています。基礎の基礎から丁寧に解説しますのでご安心ください。",
    "order": 1,
    "featured": true,
    "tags": ["初心者", "経験不要"],
    "relatedFaqIds": ["faq-002", "faq-010"],
    "createdAt": "2025-01-01T00:00:00Z",
    "updatedAt": "2025-01-01T00:00:00Z"
  },
  {
    "id": "faq-002",
    "category": "content",
    "categoryLabel": "カリキュラム",
    "question": "すでに投資経験がある場合でも役立ちますか？",
    "answer": "はい。独学で学ばれた方でも、「体系的に学び直したい」「知識の抜け漏れを確認したい」という方に好評です。実践編・応用編では中級者向けの内容も扱います。",
    "order": 2,
    "featured": false,
    "tags": ["中級者", "経験者"],
    "relatedFaqIds": ["faq-001"],
    "createdAt": "2025-01-01T00:00:00Z",
    "updatedAt": "2025-01-01T00:00:00Z"
  },
  {
    "id": "faq-003",
    "category": "fee",
    "categoryLabel": "料金について",
    "question": "受講料はいくらですか？",
    "answer": "全15回のカリキュラムで298,000円（税込）です。分割払い（月々25,000円〜）も可能です。※無料体験セミナーは完全無料です。",
    "order": 1,
    "featured": true,
    "tags": ["料金", "支払い"],
    "relatedFaqIds": ["faq-004", "faq-005"],
    "createdAt": "2025-01-01T00:00:00Z",
    "updatedAt": "2025-01-01T00:00:00Z"
  },
  {
    "id": "faq-004",
    "category": "fee",
    "categoryLabel": "料金について",
    "question": "教材費は別途必要ですか？",
    "answer": "いいえ、受講料に含まれています。テキスト、動画教材、ワークシートなど、すべての教材費込みです。追加費用は一切かかりません。",
    "order": 2,
    "featured": false,
    "tags": ["料金", "教材"],
    "relatedFaqIds": ["faq-003"],
    "createdAt": "2025-01-01T00:00:00Z",
    "updatedAt": "2025-01-01T00:00:00Z"
  },
  {
    "id": "faq-005",
    "category": "fee",
    "categoryLabel": "料金について",
    "question": "教育訓練給付金は使えますか？",
    "answer": "はい、本講座は厚生労働省の教育訓練給付金制度の対象です。条件を満たす方は受講料の20%（最大10万円）が支給されます。詳しくはお問い合わせください。",
    "order": 3,
    "featured": false,
    "tags": ["料金", "給付金"],
    "relatedFaqIds": ["faq-003"],
    "createdAt": "2025-01-01T00:00:00Z",
    "updatedAt": "2025-01-01T00:00:00Z"
  }
]
```

### 3-5. settings.json

```json
{
  "site": {
    "name": "株式投資スクール",
    "description": "投資を、学ぶ力に変える。初心者から始める株式投資スクール。実践的な15回カリキュラムで、自分で判断できる投資家へ。",
    "url": "https://example.com",
    "ogImage": "/images/og-image.jpg"
  },
  "contact": {
    "phone": "0120-XXX-XXX",
    "email": "info@example.com",
    "businessHours": "平日 10:00-18:00"
  },
  "stats": {
    "totalStudents": 12500,
    "satisfactionRate": 94.3,
    "continuationRate": 87,
    "partnerCompanies": 15
  },
  "social": {
    "twitter": "https://twitter.com/example",
    "instagram": "https://instagram.com/example",
    "facebook": "https://facebook.com/example",
    "youtube": "https://youtube.com/@example",
    "line": "https://line.me/R/ti/p/@example"
  },
  "features": {
    "blogEnabled": true,
    "caseStudyEnabled": true,
    "multiLanguage": false
  }
}
```

---

## 4. データ読み込みユーティリティ

### 4-1. データローダー（lib/data-loader.ts）

```ts
// lib/data-loader.ts
import { promises as fs } from 'fs';
import path from 'path';
import type {
  Event,
  Lesson,
  Review,
  FAQ,
  Textbook,
  Instructor,
  Media,
  CaseStudy,
  BlogPost,
  Settings,
} from '@/types';

const DATA_DIR = path.join(process.cwd(), 'data');

/**
 * JSONファイルを読み込む汎用関数
 */
async function loadJSON<T>(filename: string): Promise<T> {
  const filePath = path.join(DATA_DIR, filename);
  const fileContents = await fs.readFile(filePath, 'utf8');
  return JSON.parse(fileContents) as T;
}

/**
 * 開催スケジュールを取得
 */
export async function getEvents(): Promise<Event[]> {
  return loadJSON<Event[]>('events.json');
}

/**
 * 特定のイベントを取得
 */
export async function getEventById(id: string): Promise<Event | undefined> {
  const events = await getEvents();
  return events.find((event) => event.id === id);
}

/**
 * 利用可能なイベントのみ取得（ステータス・日付でフィルタ）
 */
export async function getAvailableEvents(): Promise<Event[]> {
  const events = await getEvents();
  const now = new Date();
  
  return events.filter((event) => {
    const eventDate = new Date(event.date);
    return (
      eventDate >= now &&
      (event.status === 'available' || event.status === 'few')
    );
  });
}

/**
 * カリキュラムを取得
 */
export async function getLessons(): Promise<Lesson[]> {
  return loadJSON<Lesson[]>('lessons.json');
}

/**
 * フェーズ別にカリキュラムを取得
 */
export async function getLessonsByPhase(
  phase: 'basic' | 'practical' | 'advanced'
): Promise<Lesson[]> {
  const lessons = await getLessons();
  return lessons.filter((lesson) => lesson.phase === phase);
}

/**
 * 受講生の声を取得
 */
export async function getReviews(): Promise<Review[]> {
  return loadJSON<Review[]>('reviews.json');
}

/**
 * おすすめの受講生の声を取得
 */
export async function getFeaturedReviews(limit: number = 3): Promise<Review[]> {
  const reviews = await getReviews();
  return reviews
    .filter((review) => review.featured)
    .sort((a, b) => a.order - b.order)
    .slice(0, limit);
}

/**
 * カテゴリ別に受講生の声を取得
 */
export async function getReviewsByCategory(
  category: string
): Promise<Review[]> {
  const reviews = await getReviews();
  return reviews.filter((review) => review.categories.includes(category as any));
}

/**
 * FAQを取得
 */
export async function getFAQs(): Promise<FAQ[]> {
  return loadJSON<FAQ[]>('faq.json');
}

/**
 * カテゴリ別にFAQを取得
 */
export async function getFAQsByCategory(category: string): Promise<FAQ[]> {
  const faqs = await getFAQs();
  return faqs
    .filter((faq) => faq.category === category)
    .sort((a, b) => a.order - b.order);
}

/**
 * おすすめのFAQを取得
 */
export async function getFeaturedFAQs(limit: number = 5): Promise<FAQ[]> {
  const faqs = await getFAQs();
  return faqs
    .filter((faq) => faq.featured)
    .sort((a, b) => a.order - b.order)
    .slice(0, limit);
}

/**
 * 教材を取得
 */
export async function getTextbooks(): Promise<Textbook[]> {
  const textbooks = await loadJSON<Textbook[]>('textbooks.json');
  return textbooks.sort((a, b) => a.order - b.order);
}

/**
 * 講師情報を取得
 */
export async function getInstructors(): Promise<Instructor[]> {
  const instructors = await loadJSON<Instructor[]>('instructors.json');
  return instructors.sort((a, b) => a.order - b.order);
}

/**
 * おすすめの講師を取得
 */
export async function getFeaturedInstructors(): Promise<Instructor[]> {
  const instructors = await getInstructors();
  return instructors.filter((instructor) => instructor.featured);
}

/**
 * メディア掲載実績を取得
 */
export async function getMedia(): Promise<Media[]> {
  const media = await loadJSON<Media[]>('media.json');
  return media.sort((a, b) => a.order - b.order);
}

/**
 * 成功事例を取得
 */
export async function getCaseStudies(): Promise<CaseStudy[]> {
  const caseStudies = await loadJSON<CaseStudy[]>('case-studies.json');
  return caseStudies.sort((a, b) => a.order - b.order);
}

/**
 * ブログ記事一覧を取得
 */
export async function getBlogPosts(): Promise<BlogPost[]> {
  const posts = await loadJSON<BlogPost[]>('blog-posts.json');
  return posts.sort(
    (a, b) =>
      new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime()
  );
}

/**
 * おすすめのブログ記事を取得
 */
export async function getFeaturedBlogPosts(limit: number = 3): Promise<BlogPost[]> {
  const posts = await getBlogPosts();
  return posts.filter((post) => post.featured).slice(0, limit);
}

/**
 * カテゴリ別にブログ記事を取得
 */
export async function getBlogPostsByCategory(
  category: string
): Promise<BlogPost[]> {
  const posts = await getBlogPosts();
  return posts.filter((post) => post.category === category);
}

/**
 * スラッグからブログ記事を取得
 */
export async function getBlogPostBySlug(
  slug: string
): Promise<BlogPost | undefined> {
  const posts = await getBlogPosts();
  return posts.find((post) => post.slug === slug);
}

/**
 * サイト設定を取得
 */
export async function getSettings(): Promise<Settings> {
  return loadJSON<Settings>('settings.json');
}
```

---

## 5. Zod バリデーション

### 5-1. バリデーションスキーマ（lib/validators.ts）

```ts
// lib/validators.ts
import { z } from 'zod';

/**
 * Event バリデーション
 */
export const EventSchema = z.object({
  id: z.string(),
  title: z.string().min(1, '開催名は必須です'),
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, '日付形式が不正です'),
  startTime: z.string().regex(/^\d{2}:\d{2}$/, '時刻形式が不正です'),
  endTime: z.string().regex(/^\d{2}:\d{2}$/, '時刻形式が不正です'),
  location: z.enum(['online', 'tokyo', 'osaka']),
  venue: z.string(),
  instructor: z.string(),
  capacity: z.number().int().positive(),
  remainingSeats: z.number().int().min(0),
  status: z.enum(['available', 'few', 'full', 'closed']),
  description: z.string().optional(),
  entryUrl: z.string().url(),
  tags: z.array(z.string()).optional(),
  createdAt: z.string(),
  updatedAt: z.string(),
});

/**
 * Lesson バリデーション
 */
export const LessonSchema = z.object({
  no: z.number().int().min(1).max(15),
  phase: z.enum(['basic', 'practical', 'advanced']),
  title: z.string().min(1),
  summary: z.string(),
  description: z.string(),
  learningObjectives: z.array(z.string()),
  outcomes: z.array(z.string()),
  topics: z.array(z.string()),
  tools: z.array(z.string()).optional(),
  duration: z.number().int().positive(),
  order: z.number().int().positive(),
});

/**
 * Review バリデーション
 */
export const ReviewSchema = z.object({
  id: z.string(),
  name: z.string().min(1),
  age: z.number().int().positive().optional(),
  occupation: z.string(),
  photo: z.string().optional(),
  rating: z.number().int().min(1).max(5),
  commentShort: z.string().max(150),
  commentLong: z.string().max(1000),
  beforeStatus: z.string(),
  afterStatus: z.string(),
  categories: z.array(
    z.enum(['beginner', 'intermediate', 'female', 'businessman', 'self-employed'])
  ),
  attendancePeriod: z.string().optional(),
  featured: z.boolean(),
  order: z.number().int().positive(),
  publishedAt: z.string(),
});

/**
 * FAQ バリデーション
 */
export const FAQSchema = z.object({
  id: z.string(),
  category: z.enum(['fee', 'content', 'format', 'support', 'refund', 'other']),
  categoryLabel: z.string(),
  question: z.string().min(1),
  answer: z.string().min(1),
  order: z.number().int().positive(),
  featured: z.boolean(),
  tags: z.array(z.string()).optional(),
  relatedFaqIds: z.array(z.string()).optional(),
  createdAt: z.string(),
  updatedAt: z.string(),
});

/**
 * Settings バリデーション
 */
export const SettingsSchema = z.object({
  site: z.object({
    name: z.string(),
    description: z.string(),
    url: z.string().url(),
    ogImage: z.string(),
  }),
  contact: z.object({
    phone: z.string(),
    email: z.string().email(),
    businessHours: z.string(),
  }),
  stats: z.object({
    totalStudents: z.number().int().positive(),
    satisfactionRate: z.number().min(0).max(100),
    continuationRate: z.number().min(0).max(100),
    partnerCompanies: z.number().int().positive(),
  }),
  social: z.object({
    twitter: z.string().url().optional(),
    facebook: z.string().url().optional(),
    instagram: z.string().url().optional(),
    youtube: z.string().url().optional(),
    line: z.string().url().optional(),
  }),
  features: z.object({
    blogEnabled: z.boolean(),
    caseStudyEnabled: z.boolean(),
    multiLanguage: z.boolean(),
  }),
});

/**
 * バリデーション実行ヘルパー
 */
export function validateData<T>(
  schema: z.ZodSchema<T>,
  data: unknown
): { success: true; data: T } | { success: false; errors: z.ZodError } {
  const result = schema.safeParse(data);
  
  if (result.success) {
    return { success: true, data: result.data };
  } else {
    return { success: false, errors: result.error };
  }
}
```

---

## 6. 使用例（Next.js コンポーネント）

### 6-1. スケジュールページ

```tsx
// pages/schedule/index.tsx
import { getAvailableEvents, getSettings } from '@/lib/data-loader';
import { Event } from '@/types';

export const metadata = {
  title: '開催スケジュール | 株式投資スクール',
  description: '無料体験セミナーの開催スケジュール一覧',
};

export default async function SchedulePage() {
  const events = await getAvailableEvents();
  const settings = await getSettings();
  
  return (
    <main className="py-16 md:py-24 bg-gray-50">
      <div className="max-w-screen-lg mx-auto px-4">
        <h1 className="text-3xl md:text-4xl font-semibold text-center text-gray-800 mb-12">
          無料体験セミナー 開催スケジュール
        </h1>
        
        {/* スケジュール一覧 */}
        <div className="space-y-4">
          {events.map((event) => (
            <EventCard key={event.id} event={event} />
          ))}
        </div>
        
        {/* 空の場合 */}
        {events.length === 0 && (
          <div className="text-center py-12">
            <p className="text-gray-600 mb-4">
              現在募集中のセミナーはありません
            </p>
            <p className="text-sm text-gray-500">
              お問い合わせ：
              <a href={`tel:${settings.contact.phone}`} className="text-accent">
                {settings.contact.phone}
              </a>
            </p>
          </div>
        )}
      </div>
    </main>
  );
}

// イベントカードコンポーネント
function EventCard({ event }: { event: Event }) {
  const statusColor = {
    available: 'text-green-500',
    few: 'text-amber-500',
    full: 'text-red-500',
    closed: 'text-gray-500',
  };
  
  const statusLabel = {
    available: '◯ 余裕あり',
    few: '△ 残りわずか',
    full: '× 満席',
    closed: '募集終了',
  };
  
  return (
    <div className="bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow">
      <div className="flex flex-col md:flex-row md:items-center md:justify-between">
        {/* 左側：日程情報 */}
        <div className="flex-1 mb-4 md:mb-0">
          <div className="flex items-center gap-4 mb-2">
            <span className="text-xl font-semibold text-gray-800">
              {new Date(event.date).toLocaleDateString('ja-JP', {
                month: 'long',
                day: 'numeric',
                weekday: 'short',
              })}
            </span>
            <span className="text-lg text-gray-600">
              {event.startTime}-{event.endTime}
            </span>
            <span
              className={`px-3 py-1 ${
                event.location === 'online' ? 'bg-accent' : 'bg-gray-600'
              } text-white text-sm rounded-full`}
            >
              {event.venue}
            </span>
          </div>
          
          <div className="flex items-center gap-4 text-sm text-gray-600">
            <span>👨‍🏫 {event.instructor}</span>
            <span className={`font-medium ${statusColor[event.status]}`}>
              {statusLabel[event.status]}
            </span>
          </div>
        </div>
        
        {/* 右側：CTAボタン */}
        {event.status !== 'full' && event.status !== 'closed' && (
          <a
            href={event.entryUrl}
            className="inline-block bg-accent text-white text-base font-semibold 
                       py-3 px-6 rounded-lg hover:opacity-90 transition-opacity"
          >
            この日程で申し込む
          </a>
        )}
      </div>
    </div>
  );
}
```

### 6-2. カリキュラムページ

```tsx
// pages/curriculum/index.tsx
import { getLessons, getLessonsByPhase } from '@/lib/data-loader';

export const metadata = {
  title: 'カリキュラム詳細 | 株式投資スクール',
  description: '15回で完成する実践的カリキュラム',
};

export default async function CurriculumPage() {
  const basicLessons = await getLessonsByPhase('basic');
  const practicalLessons = await getLessonsByPhase('practical');
  const advancedLessons = await getLessonsByPhase('advanced');
  
  return (
    <main className="py-16 md:py-24 bg-white">
      <div className="max-w-screen-lg mx-auto px-4">
        <h1 className="text-3xl md:text-4xl font-bold text-center text-gray-800 mb-4">
          15回で完成する、実践的カリキュラム
        </h1>
        <p className="text-lg text-center text-gray-600 mb-12">
          初心者が投資家として自立するための、体系的な学習プログラム
        </p>
        
        {/* タイムライン */}
        <div className="space-y-12">
          {/* 基礎編 */}
          <PhaseSection
            title="基礎編（第1〜5回）"
            description="投資の基本知識、市場の仕組み、リスク管理の考え方を学びます"
            lessons={basicLessons}
            color="bg-blue-500"
          />
          
          {/* 実践編 */}
          <PhaseSection
            title="実践編（第6〜10回）"
            description="実際の銘柄分析、シミュレーション投資で実践経験を積みます"
            lessons={practicalLessons}
            color="bg-green-500"
          />
          
          {/* 応用編 */}
          <PhaseSection
            title="応用編（第11〜15回）"
            description="投資戦略の立て方、経済ニュースの読み解き方を習得します"
            lessons={advancedLessons}
            color="bg-purple-500"
          />
        </div>
      </div>
    </main>
  );
}

function PhaseSection({
  title,
  description,
  lessons,
  color,
}: {
  title: string;
  description: string;
  lessons: Lesson[];
  color: string;
}) {
  return (
    <div className="border-l-4 border-accent pl-6">
      <h2 className="text-2xl font-semibold text-gray-800 mb-2">{title}</h2>
      <p className="text-gray-600 mb-6">{description}</p>
      
      <div className="space-y-4">
        {lessons.map((lesson) => (
          <div key={lesson.no} className="relative">
            {/* 番号バッジ */}
            <div
              className={`absolute -left-9 top-0 w-6 h-6 ${color} text-white 
                          rounded-full flex items-center justify-center 
                          text-sm font-semibold`}
            >
              {lesson.no}
            </div>
            
            <div className="bg-gray-50 rounded-lg p-4">
              <h4 className="text-lg font-semibold text-gray-800 mb-1">
                {lesson.title}
              </h4>
              <p className="text-sm text-gray-600 mb-2">{lesson.summary}</p>
              
              {/* トピック */}
              <div className="flex flex-wrap gap-2">
                {lesson.topics.map((topic, index) => (
                  <span
                    key={index}
                    className="px-2 py-1 bg-white text-xs text-gray-600 rounded"
                  >
                    {topic}
                  </span>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

### 6-3. トップページ（実績セクション）

```tsx
// pages/index.tsx
import { getSettings, getFeaturedReviews } from '@/lib/data-loader';

export default async function HomePage() {
  const settings = await getSettings();
  const reviews = await getFeaturedReviews(3);
  
  return (
    <main>
      {/* 実績セクション */}
      <section className="py-12 md:py-16 bg-gray-50">
        <div className="max-w-screen-lg mx-auto px-4">
          <h2 className="text-2xl md:text-3xl font-semibold text-center text-gray-800 mb-8">
            多くの方に選ばれています
          </h2>
          
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-6">
            <StatsCard
              value={settings.stats.totalStudents.toLocaleString()}
              unit="名"
              label="累計受講者数"
            />
            <StatsCard
              value={settings.stats.satisfactionRate}
              unit="%"
              label="満足度"
              color="text-green-500"
            />
            <StatsCard
              value={settings.stats.continuationRate}
              unit="%"
              label="受講継続率"
            />
            <StatsCard
              value={settings.stats.partnerCompanies}
              unit="社"
              label="提携金融機関"
            />
          </div>
        </div>
      </section>
      
      {/* 受講生の声セクション */}
      <section className="py-16 md:py-24 bg-white">
        <div className="max-w-screen-lg mx-auto px-4">
          <h2 className="text-3xl md:text-4xl font-semibold text-center text-gray-800 mb-12">
            受講生の声
          </h2>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {reviews.map((review) => (
              <ReviewCard key={review.id} review={review} />
            ))}
          </div>
        </div>
      </section>
    </main>
  );
}

function StatsCard({
  value,
  unit,
  label,
  color = 'text-accent',
}: {
  value: string | number;
  unit: string;
  label: string;
  color?: string;
}) {
  return (
    <div className="bg-white rounded-xl p-6 text-center shadow-sm">
      <div className={`text-3xl md:text-4xl font-bold ${color} mb-2`}>
        {value}
        <span className="text-lg">{unit}</span>
      </div>
      <div className="text-sm text-gray-600">{label}</div>
    </div>
  );
}
```

---

## 7. 拡張性・管理メモ

### JSON 更新フロー

1. **ローカルで JSON ファイルを編集**
2. **Git にコミット＆プッシュ**
3. **Vercel が自動でビルド・デプロイ**（1〜2 分）

### 表示順制御

- `order` フィールドで昇順ソート
- `featured` フラグでトップページ表示制御
- `publishedAt` で新着順ソート（ブログ等）

### 今後の API 化対応

現在の JSON ファイル構造をそのまま API レスポンスとして返すことが可能：

```ts
// pages/api/events.ts
import { getEvents } from '@/lib/data-loader';
import { NextResponse } from 'next/server';

export async function GET() {
  const events = await getEvents();
  return NextResponse.json(events);
}
```

### 画像最適化

```tsx
// public/images に配置
import Image from 'next/image';

<Image
  src="/images/reviews/tanaka.jpg"
  alt="田中由美様"
  width={400}
  height={400}
  className="w-16 h-16 rounded-full object-cover"
/>
```

### カテゴリ別絞り込み

```tsx
// FAQのカテゴリ別表示例
const faqCategories = [
  { id: 'fee', label: '料金について' },
  { id: 'content', label: 'カリキュラム' },
  { id: 'format', label: '受講形式' },
  { id: 'support', label: 'サポート' },
  { id: 'refund', label: '返金・解約' },
];

// カテゴリごとにFAQを取得
const categorizedFAQs = await Promise.all(
  faqCategories.map(async (cat) => ({
    category: cat,
    items: await getFAQsByCategory(cat.id),
  }))
);
```

---

## 8. データ更新ガイドライン

### 8-1. 更新頻度の目安

| データ       | 更新頻度 | 担当者           | 注意点                     |
| ------------ | -------- | ---------------- | -------------------------- |
| events       | 週次     | 運営チーム       | 2週間先まで常に登録        |
| reviews      | 月次     | マーケティング   | 写真掲載の許諾取得必須     |
| faq          | 月次     | カスタマーサポート | 実際の問い合わせを反映     |
| blog-posts   | 週 1〜2 回 | コンテンツチーム | SEO キーワードを意識       |
| settings     | 低       | 運営チーム       | 実績数値は根拠を持って更新 |

### 8-2. データ更新時のチェックリスト

- [ ] JSON 形式が正しいか（`,` の位置、`}` の閉じ忘れ）
- [ ] 必須フィールドがすべて入力されているか
- [ ] 日付形式が正しいか（`YYYY-MM-DD`）
- [ ] URL が正しいか（http/https から始まる）
- [ ] 画像ファイルが存在するか（`/public/images` に配置済み）
- [ ] order/no フィールドで表示順序が適切か
- [ ] テスト環境で表示確認

### 8-3. 画像命名規則

```
/public/images/
├── instructors/        # 講師写真
│   ├── yamada.jpg
│   └── sato.jpg
├── reviews/            # 受講生写真
│   ├── tanaka.jpg
│   └── suzuki.jpg
├── textbooks/          # 教材写真
│   ├── textbook-1.jpg
│   └── textbook-2.jpg
├── media-logos/        # メディアロゴ
│   ├── nikkei.png
│   └── nhk.png
└── blog/               # ブログアイキャッチ
    ├── 2025-01-01-investment-basics.jpg
    └── 2025-01-15-nisa-guide.jpg
```

**命名ルール**：

- 小文字・ハイフン区切り
- 日本語は使用しない
- 日付を含める（ブログのみ）

---

## 9. 次フェーズ（ルーティング設計）で決める項目リスト

### 必須決定事項

- [ ] **ページルーティング構造**
  - Pages Router の ディレクトリ構成
  - 動的ルート（`[slug]`、`[id]`）の使用箇所
  - パンくずリスト用の階層定義

- [ ] **データフェッチング戦略**
  - SSG（Static Site Generation）対象ページ
  - ISR（Incremental Static Regeneration）の revalidate 時間
  - クライアントサイドフェッチの有無

- [ ] **API Routes の必要性**
  - フォーム送信用エンドポイント
  - 検索・フィルタリング用エンドポイント
  - 外部サービス連携（メール送信等）

### 推奨決定事項

- [ ] **メタデータ管理**
  - 各ページの title/description テンプレート
  - OGP 画像の自動生成ルール
  - 構造化データ（Schema.org）の実装範囲

- [ ] **エラーハンドリング**
  - 404 ページのデザイン
  - エラーページ（error.tsx）の実装
  - データ取得失敗時のフォールバック

- [ ] **リダイレクト・リライト**
  - 旧 URL からのリダイレクト
  - トレイリングスラッシュの扱い
  - www あり/なしの統一

### 後回し可能な項目

- [ ] 多言語対応（i18n）
- [ ] サイトマップ XML の自動生成
- [ ] RSS フィード生成
- [ ] PWA 対応

---

## 付録：データ管理ツール

### JSON バリデーションスクリプト

```ts
// scripts/validate-data.ts
import { promises as fs } from 'fs';
import path from 'path';
import {
  EventSchema,
  LessonSchema,
  ReviewSchema,
  FAQSchema,
  SettingsSchema,
} from '@/lib/validators';

async function validateAllData() {
  const dataDir = path.join(process.cwd(), 'data');
  
  // events.json
  const eventsData = JSON.parse(
    await fs.readFile(path.join(dataDir, 'events.json'), 'utf8')
  );
  eventsData.forEach((event: any, index: number) => {
    const result = EventSchema.safeParse(event);
    if (!result.success) {
      console.error(`❌ events.json [${index}]:`, result.error.errors);
    } else {
      console.log(`✅ events.json [${index}]: OK`);
    }
  });
  
  // lessons.json
  const lessonsData = JSON.parse(
    await fs.readFile(path.join(dataDir, 'lessons.json'), 'utf8')
  );
  lessonsData.forEach((lesson: any, index: number) => {
    const result = LessonSchema.safeParse(lesson);
    if (!result.success) {
      console.error(`❌ lessons.json [${index}]:`, result.error.errors);
    } else {
      console.log(`✅ lessons.json [${index}]: OK`);
    }
  });
  
  // reviews.json, faq.json, settings.json も同様
  
  console.log('\n🎉 バリデーション完了');
}

validateAllData().catch(console.error);
```

```json
// package.json
{
  "scripts": {
    "validate": "tsx scripts/validate-data.ts"
  }
}
```

---

**📌 備考**

- このデータ設計はローカル JSON ファイルでの静的管理を前提としています
- CMS を導入する場合でも、同じ型定義・構造を使用できます
- Zod バリデーションでデータ品質を保証します
- Next.js の SSG/ISR で高速なページ生成が可能です

---

**🔗 関連ドキュメント**

- `要件定義.md`（前提文書）
- `情報設計_サイトマップ.md`（前提文書）
- `コンテンツ設計.md`（前提文書）
- `デザイン指針.md`（前提文書）
- `ルーティング設計.md`（次フェーズで作成）
- `実装ガイド.md`（次フェーズで作成）

